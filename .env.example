# Content source: 'static' (local TS files) | 'wordpress' (Headless CMS)
CONTENT_SOURCE=static

# WordPress Headless CMS (used when CONTENT_SOURCE=wordpress)
# WORDPRESS_API_URL=https://example.com/wp-json
# WORDPRESS_API_TOKEN=
